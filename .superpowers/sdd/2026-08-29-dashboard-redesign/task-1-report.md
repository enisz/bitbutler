# Task 1 Report: Pie-chart data model + selector

## Status: DONE_WITH_CONCERNS

## What was implemented

Exactly the 6 steps in the brief, scoped to the three named files:

1. `packages/app/src/app/models/dashboard.model.ts`
   - Widened `WidgetTypeId` to `'stat-tile' | 'torrent-list' | 'pie-chart'`.
   - Added `PieChartGroupBy`, `PieChartConfig` (after `TorrentListConfig`).
   - Widened `WidgetConfig` to include `PieChartConfig`.
   - Added `PieChartSlice`, `PieChartData` (after `TorrentListData`, end of file).
2. `packages/app/src/app/pages/dashboard/widget-selectors.ts`
   - Imported `PieChartConfig`, `PieChartData`, `PieChartSlice`.
   - Added `PieStateBucket` type, `PIE_STATE_BUCKETS` map, `PIE_STATE_BUCKET_ORDER` array (after `ACTIVE_STATES`).
   - Added `selectPieChartData()` (after `selectTorrentListData`).
   - Updated `resolveWidgetData()` to dispatch `'pie-chart'` -> `selectPieChartData` and widened its return type.
3. `packages/app/src/app/pages/dashboard/widget-selectors.spec.ts`
   - Added `selectPieChartData` to the import.
   - Added `describe('selectPieChartData', ...)` block (3 tests: state-bucket partition, empty list, category grouping) before `describe('resolveWidgetData', ...)`.
   - Added the pie-chart dispatch case to the `resolveWidgetData` describe block.

All code was copied verbatim from the brief - no deviation from the given TypeScript/test code.

## TDD evidence

### RED

Command: `npx ng test --watch=false --include="**/widget-selectors.spec.ts"` (see "Test command" note below for why the brief's literal command doesn't work in this environment), run after Step 1 (tests written) and before Step 3/4 (model/selector implemented):

```
✘ [ERROR] TS2305: Module '"./widget-selectors"' has no exported member 'selectPieChartData'.
    src/app/pages/dashboard/widget-selectors.spec.ts:5:2

✘ [ERROR] TS2322: Type '"pie-chart"' is not assignable to type 'WidgetTypeId'.
    src/app/pages/dashboard/widget-selectors.spec.ts:256:6

✘ [ERROR] TS2353: Object literal may only specify known properties, and 'groupBy' does not exist in type 'WidgetConfig'.
    src/app/pages/dashboard/widget-selectors.spec.ts:261:16
```

Matches the brief's expected failure exactly.

### GREEN

After implementing Steps 3-4, ran the tests via a direct vitest invocation (bypassing the Angular whole-app builder - see concern below for why):

Command: `npx vitest run src/app/pages/dashboard/widget-selectors.spec.ts --config vitest-base.config.mts --globals`

```
 Test Files  1 passed (1)
      Tests  16 passed (16)
   Start at  22:21:09
   Duration  186ms
```

16 = the 12 pre-existing tests (`selectStatTileData`, `selectTorrentListData`, original `resolveWidgetData` cases) + the 4 new ones (3 in `selectPieChartData`, 1 new `resolveWidgetData` case). Re-ran once more after the commit (post lint-staged/prettier auto-format) for a final clean confirmation - same result, 16/16 passing.

## Files changed

- `packages/app/src/app/models/dashboard.model.ts`
- `packages/app/src/app/pages/dashboard/widget-selectors.ts`
- `packages/app/src/app/pages/dashboard/widget-selectors.spec.ts`

Commit: `727d35f` - `#324: add pie-chart data model and selector`

## Self-review

- **Completeness:** All 6 steps done. `git diff` of the commit shows only the three files, all edits from the brief present (model widening, selector, tests).
- **Quality:** `selectPieChartData` follows the existing `selectStatTileData`/`selectTorrentListData` pattern (plain function taking `snapshot, config`, returning a typed result). Comments (bucket-overlap note, `labelKey` doc) copied verbatim from the brief. `eslint --max-warnings=0 --fix` and `prettier --write` ran clean via the pre-commit hook (lint-staged) with no reported issues.
- **Discipline:** Nothing beyond the brief was touched. Verified `git add`/`git status` showed only the three named files staged before committing; a stray `packages/app/out-tsc/` directory generated by my own `tsc -b` verification runs was deleted before staging so it wouldn't leak into the commit.
- **Testing:** Tests are the verbatim ones specified, which do verify real behavior (bucket partition ordering/omission of empty buckets, category `-` fallback, dispatch wiring). RED captured before GREEN. Output is pristine (16 passed, 0 failed, no warnings).

## Concerns

1. **The brief's literal test command doesn't run as written in this environment.** `npm test --workspace=@bitbutler/app -- widget-selectors` fails with `Error: Unknown argument: watch` - the `ng test` yargs parser mishandles a positional arg alongside `--watch=false` here. Worked around with `--include="**/widget-selectors.spec.ts"` (or a direct `vitest run <path> --config vitest-base.config.mts --globals` for the final clean run). This is an environment/tooling quirk unrelated to my changes - worth flagging to whoever owns later tasks' briefs, since they likely specify the same command form.

2. **Step 6's assumption ("nothing should break, since no other file switches exhaustively on `WidgetTypeId`/`WidgetConfig` yet") is false in the current repo state**, and this is the more important finding:
   - `packages/app/src/app/pages/dashboard/widget-catalog.ts` (already committed on this branch, prior to this SDD plan, commit `53dbe38`) declares `WIDGET_CATALOG: Record<WidgetTypeId, WidgetCatalogMeta>` with only `'stat-tile'` and `'torrent-list'` entries. Widening `WidgetTypeId` makes this a `TS2741: Property "pie-chart" is missing` error.
   - `packages/app/src/app/pages/dashboard/dashboard.ts` (currently has **uncommitted** local changes on disk, pre-existing before I started, not part of this task) has a field typed `value: StatTileData | TorrentListData` (line 74) that a computed signal assigns `resolveWidgetData(...)`'s result into (line 249) - now a `TS2322` mismatch since that union doesn't include `PieChartData`.
   - I confirmed via `npx tsc -b packages/app --noEmit` that these are the **only** three errors in the whole project after my change - nothing in `dashboard.model.ts`, `widget-selectors.ts`, or `widget-selectors.spec.ts` is implicated. I did not touch `widget-catalog.ts` or `dashboard.ts`, per the brief's explicit file scope.
   - This is why the "GREEN" evidence above used a direct `vitest run` rather than `ng test`/`npm run build` as Step 5/6 prescribe: those commands always build the whole app (the Angular vitest builder's `buildTarget` is the full app, not just matched spec files), so they fail right now regardless of which spec filter is passed, purely due to the two files above.
   - I found the full 10-task plan at `docs/superpowers/plans/2026-08-29-dashboard-redesign.md` and confirmed a later task (around line 1036-1091 in that doc) is scoped to update `widget-catalog.ts` (and `widget-catalog.spec.ts`) to add the `'pie-chart'` entry, and another later task touches `dashboard.ts`. So this breakage is presumably expected to self-resolve once those later tasks land - but it means **the whole-app build/test suite will not be green between now and whichever task fixes `widget-catalog.ts`/`dashboard.ts`**. Flagging this so the controller can decide whether that's acceptable given the "review once at the end of the plan" policy, or whether task ordering/expectations need adjusting.
