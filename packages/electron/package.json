{
  "name": "@bitbutler/electron",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@bitbutler/shared": "*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.preload.json",
    "test": "vitest run"
  }
}
