{
  "name": "@bitbutler/electron",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "node --eval \"const fs=require('fs');if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true});fs.cpSync('src','dist',{recursive:true});\""
  }
}
