import { TooltipOverflow } from './tooltip-overflow';

describe('TooltipOverflow', () => {
  it('should create an instance', () => {
    const directive = new TooltipOverflow();
    expect(directive).toBeTruthy();
  });
});
