import { SpeedLimitPipe } from './speed-limit-pipe';

describe('SpeedLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeedLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
